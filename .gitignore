.vscode
out
esp
serial*.log
serial*.txt
.vs
.idea/
.trae
out/draw.o
/esp
/mnt
/out
/xlibc
/open-vmdk-master
/compile_commands.json
.cache
.config
.ninja_deps
.ninja_log
__pycache__/
*.py[cod]
include/generated/
kernel/build_config.h

bash-*

*.img
*.vmdk
/XJ380-installer.iso
*.tags
*.elf
*.o
*.d
*.xz
*.zip
!Bf/
!Bf/**
Bf/usr/
Bf/var/
Bf/xbps-cache/
Bf/xbps-root/
2.txt
out.txt
nvme.txt
ide.txt
SATA.txt
scsi.txt

# will generate by Makefile
.clangd

build.ninja
# ARM32 (Zynq-7020) 构建图,与 x86 图并存
build-arm.ninja

.codex

XJ380/

venv/

# Local audit notes
/docs/LICENSE_AUDIT_2026-08-08.md

# 板级参考资料(原理图 PDF、Vivado 导出的 XSA):体积大且是二进制,
# 不适合进版本库。需要时放在本地即可;确实要提交用 git add -f。
/other-infos/

# Vivado/Vitis 从 XSA 生成的文件(ps7_init.c 一万多行,design_1.hwh 是
# 硬件描述)。JTAG 流程实际需要的只有同目录下的 ps7_init_uart1.tcl。
/tmp-test/zynq/ps7_init_uart1.c
/tmp-test/zynq/design_1.hwh

# --- 本机配置(见仓库根目录 config.py 顶部说明)------------------------------
# config.py 本身是要提交的(里面只有通用/占位值)。若你不想让自己的机器路径
# 进版本库,就 `cp config.example.py config.local.py` 后只改 config.local.py ——
# 它被忽略,而 config.py 会自动读它覆盖。
/config.local.py

# 由 `python config.py` 生成,给 JTAG 的 Tcl 脚本 source 用。内容是本机路径。
/tmp-test/jtag/paths.tcl

# verify_board.py 在失败时留下的串口原文(含本机信息)
/verify_board_raw.txt
